import styled from 'styled-components'

export const ProductsWrap = styled.div`
  padding: 0.5rem;
  list-style: none;
  margin: -2rem;
  height: calc(100vh - 115px);
  overflow: auto;
`

export const ProductsWrapInner = styled.ul`
  margin: 0;
  padding: 0;
  display: flex;
  list-style: none;
  flex-wrap: wrap;
`
